{
  "name": "tabstyr-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node ../scripts/fetch-changelog.mjs --soft && vue-tsc --noEmit && vite build && rm -f dist/assets/*.woff && node -e \"require('fs').copyFileSync('dist/index.html','dist/404.html')\" && node scripts/prerender.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/space-grotesk": "^5.3.0",
    "@types/node": "^25.9.5",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/test-utils": "^2.5.0",
    "happy-dom": "^20.11.15",
    "playwright": "^1.62.1",
    "typescript": "^5.7.0",
    "vite": "^8.2.2",
    "vitest": "^4.1.11",
    "vue-tsc": "^3.3.11"
  },
  "comments": {
    "overrides": "brace-expansion: held on the 2.x line for minimatch@9, which imports it as a DEFAULT export. DO NOT raise this to >=5.0.8 to clear GHSA-mh99-v99m-4gvg: brace-expansion 5 removed the default export, so minimatch@9 throws '(0, brace_expansion_1.default) is not a function' at runtime while `npm audit` reports 0 vulnerabilities — a silent break that CI does not catch. Dev-only (vue-tsc / vitest tooling); never bundled into the site. Same constraint as the root package.json; see its comments.overrides."
  },
  "overrides": {
    "brace-expansion": "^2.1.2"
  }
}
